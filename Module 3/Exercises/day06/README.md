# Day 06 Exercises - React Router v6

Covers client-side routing, layout nesting (`Outlet`), dynamic parameters (`useParams`), query string syncing (`useSearchParams`), and route protection (`RequireAuth`).

## How to Run
1. `npm install`
2. `npm run dev`