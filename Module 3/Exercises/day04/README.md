# Day 04 Exercises - Side Effects (useEffect & useRef)

Covers `useEffect` lifecycle, API fetching with `AbortController`, `document.title` syncing, and auto-focusing elements with `useRef`.

## How to Run
1. `npm install`
2. `npm run dev`