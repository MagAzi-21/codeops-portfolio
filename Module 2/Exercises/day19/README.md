# Addis Market Shopping List (Day 19)

A single-page interactive shopping list application.

## How to Run
Open `index.html` in any modern web browser.

## Features
- Form submit via `preventDefault` with field validation.
- Row creation using `document.createElement` and `append`.
- Single delegated listener on `#list` for deletion and toggling `.bought`.
- Dynamic running total calculated across active elements.