# TeleBirr Transaction Report (Day 18)

A modular JavaScript application parsing transaction logs using higher-order array methods (`map`, `filter`, `reduce`), destructuring, and object spread syntax.

## How to Run
Execute directly using Node.js:

```bash
node exercises.js
node ex5_app.js
node app.js